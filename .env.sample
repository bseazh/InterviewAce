POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=interviewace

# For the app container; 'db' is the service hostname inside the Compose network
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/interviewace

